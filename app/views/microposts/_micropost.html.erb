<div id="micropost-<%= micropost.id %>">
  <%= link_to gravatar_for(micropost.user,
    size: Settings.user.avatar_size.small), micropost.user %>
  <span class="user"><%= link_to micropost.user.name, micropost.user %></span>
  <span class="content">
    <%= link_to micropost.title, micropost %>
    <!-- <%= image_tag micropost.picture.url if micropost.picture? %> -->
  </span>
  <span class="timestamp">
    Posted <%= time_ago_in_words(micropost.created_at) %> ago.
    <% if logged_in? && current_user.current_user?(micropost.user) %>
      <%= link_to t("delete"), micropost, method: :delete,
        data: { confirm: t("confirm_destroy") } %>
    <% end %>
  </span>
</div>
