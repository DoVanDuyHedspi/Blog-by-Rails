<%= link_to gravatar_for(current_user, size: Settings.user.avatar_size.small),
 current_user %>
<h1><%= current_user.name %></h1>
<span><%= pluralize(current_user.microposts.count, t("microposts")) %></span>
